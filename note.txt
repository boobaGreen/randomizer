randomNumberInRange = (randomNumber * (rangeEnd - rangeStart) / 255) + rangeStart;

allora sembra che raw_rand restituisca 31 bumeri casuali bytes e non 32 come da doc.
inoltre puo' ripetere le chiamate se non pulito.

vogli fare menu con :

- puro (senza storico) esempio tira 3 dadi (participant: 6 draw: 3)

- con storici esclusi esempi tombola (partcipants :90 - draw :90) o carte francesi (particpants 52 - draws 52 ) o per sorteggi su twitch ecc ecc

- grafico distribuzione 


curl -X POST -H "Content-Type: application/json" -d "{ }" http://bkyz2-fmaaa-aaaaa-qaaaq-cai.localhost:8000/randomness

curl -X POST -H "Content-Type: application/json" -d '{
  "participants": 10,
  "draws": 5
}' http://bkyz2-fmaaa-aaaaa-qaaaq-cai.localhost:8000/randomness